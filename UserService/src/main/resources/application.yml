spring:
  application:
    name: UserService

  datasource:
    url: jdbc:postgresql://localhost:5433/user_db
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  cloud:
    config:
      enabled: false

server:
  port: 8081

admin:
  username: palak@grocery.com
  password: Palak@123
